
import {
    Box,
    Spinner,
    Button,
    Flex,
    Text,
    Image,
} from 'theme-ui'

function WagerDetails({ accountId, activeWallet }) {

    
    
    return (
        <Box sx={{my:'2rem'}}>

            
        </Box>
    );
}

export default WagerDetails;
